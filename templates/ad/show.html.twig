{% extends 'base.html.twig'%}
{% block title %}
    <h1> {{ ad.title }}</h1>
{% endblock %}
{% block body %}
    <div class="adtitle" style ="background-image:url({{ ad.coverImage }})">
         <div class="container">
                <h1> {{ ad.title }} </h1> 
                <p> {{ad.introduction}}</p>
                 <p class="h2"> 
                  <strong> {{ad.rooms}} chambres à {{ad.price}} euros;
                   </strong> par nuit
                 </p>
                 <a href="#" class="btn btn-primary">Réserver !</a>
         </div> 
    </div>  
    <div class="container">
        <div class="row">
            <div class="col-md-8 mb-3 mr-3">
                {{ ad.content | raw }}
                <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
					<ol class="carousel-indicators">
						{% for image in ad.images %}
							<li data-target="#carouselExampleIndicators" data-slide-to="{{ loop.index0 }}" 
							{% if loop.first %}class="active"{% endif %}></li>
						{% endfor %}
					</ol>
					<div class="carousel-inner">
							{% for image in ad.images %}
								<div class="carousel-item {% if loop.first %} active {% endif %}">
									 <img src="{{image.url}}" class="d-block w-100" alt="first slide">
									 <div class="carousel-caption d-none d-md-block">
       								 	<h5>{{ ad.title }}</h5>
        								<p> {{ image.caption }} </p>
     							 	 </div>
								</div>
							{% endfor %}
					</div>
					<a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
						<span class="carousel-control-prev-icon" aria-hidden="true"></span>
						<span class="sr-only">Previous</span>
					</a>
					<a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
						<span class="carousel-control-next-icon" aria-hidden="true"></span>
						<span class="sr-only">Next</span>
					</a>
				</div>
            </div>

            <div class="col">
                <div class="row">
                    <div class="col-4">
                        <img src="http://placehold.it/64x64" alt="avatar de joseph">
                    </div>
                    <div class="col">
                        <h3> joseph du pont</h3>
                        <span> 3 annonces</span>
                    </div>
                    <p>Lorem, ipsum dolor sit amet consectetur adipisicing elit. Corrupti voluptate facilis architecto. Ex nesciunt omnis aliquam perferendis ab veritatis magnam?</p>
                    <p>Doloribus facilis iusto quaerat distinctio perferendis dignissimos. Deleniti id totam omnis. Suscipit recusandae laudantium eius eligendi voluptatum qui alias cumque.</p>
                    <p>Accusantium perferendis non illum quisquam, autem vero et repudiandae aliquam nulla officia consequatur odio laborum assumenda. Qui sapiente enim molestiae.</p>
                </div>
            </div>
        </div>
    
    </div>
{% endblock %}